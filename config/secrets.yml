# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  secret_key_base: 812007a2dd871075aef37705cc56d070becc18eddfc1126522ede9c1f7e7f0a49a004dd6af6284f0f0626b1115022a520267e5bcb74298eb0b309be6e56b4e6d

  twitter:
    consumer_key: <%= ENV['TWITTER_API_KEY'] %>
    consumer_secret: <%= ENV['TWITTER_API_SECRET'] %>
    access_token: <%= ENV['TWITTER_ACCESS_TOKEN'] %>
    access_token_secret: <%= ENV['TWITTER_ACCESS_TOKEN_SECRET'] %>

test:
  secret_key_base: 9c654c0d0e7d9cb209eb31891d4fc796bd87adb045c02bb551fa133dc8f2924cf9c63566eda6feca1e799b73bebafe8e0d3ca5ee55198f4227d6189547571ae9

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
